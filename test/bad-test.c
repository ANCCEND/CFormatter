// 错误测试文件：C语言子集语法分析

#include <stdio.h>

/* ====== 错误1：函数声明参数错误 ====== */
// 'void' 不能和其他参数混用
//int wrongFunc(void, int a);  

/* ====== 错误2：变量初始化缺失 ====== */
//int x = ;   // 缺少右侧表达式

/* ====== 错误3：缺少分号 ====== */
//int y = 10  // 少了分号

/* ====== 错误4：if 语句条件为空 ====== */
int main(void) {
    int a = 5;

   // if () {          // 条件为空
        a = a + 1;
  //  }

    /* ====== 错误5：while 语句缺少右括号 ====== */
    
    /* ====== 错误6：for 语句缺少表达式 ====== */
    for (int i = 0; ; i = i + 1) {  // 中间表达式缺失
        a = a + i;
    }

    /* ====== 错误7：未声明的变量使用 ====== */
    b = a + 1;   // b 未定义

    /* ====== 错误8：函数调用未声明 ====== */
    notDeclaredFunc();  

    /* ====== 错误9：return 缺少表达式 ====== */
    return ;   // main 返回类型是 int，这里必须返回一个值
}

/* ====== 错误10：重复定义函数 ====== */
int main(void) {
    return 0;
}
